services:
  # ============================================
  # CORE APPLICATION SERVICES
  # ============================================

  api:
    build:
      context: .
      dockerfile: Dockerfile.fastapi
    image: octopus-trading-api:latest
    container_name: octopus-api
    restart: unless-stopped
    ports:
      - "8011:8000"
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
      - ./models:/app/models
    environment:
      - ENVIRONMENT=development
      - LOG_LEVEL=INFO
      - DATABASE_URL=postgresql://postgres:${DB_PASSWORD:-postgres}@db:5432/trading_db
      - REDIS_URL=redis://redis:6379/0
      # Short secrets for Docker (bcrypt 72-byte limit). Set SECRET_KEY/JWT_SECRET_KEY in env for production.
      - SECRET_KEY=docker-dev-secret-key-32chars-minimum
      - JWT_SECRET_KEY=docker-dev-jwt-secret-key-32chars-min
      - FALCON_TGI_URL=${FALCON_TGI_URL:-}
      - FINGPT_LOCAL_URL=${FINGPT_LOCAL_URL:-}
      - HF_TOKEN=${HF_TOKEN:-}
    depends_on:
      - db
      - redis
    networks:
      - octopus-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  frontend:
    build:
      context: ./frontend-nextjs
      dockerfile: Dockerfile
      args:
        - NEXT_PUBLIC_API_URL=http://localhost:8011
    image: octopus-trading-frontend:latest
    container_name: octopus-frontend
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_API_URL=http://localhost:8011
      - NODE_ENV=production
      - NEXTAUTH_SECRET=${SECRET_KEY:-dev-secret-key}
      - NEXTAUTH_URL=http://localhost:3000
    depends_on:
      api: { condition: service_healthy }
    networks:
      - octopus-network
    # No volumes: run built standalone app from image (fully dockerized)

  # ============================================
  # DATABASE & CACHE SERVICES
  # ============================================

  db:
    image: timescale/timescaledb:latest-pg15
    container_name: octopus-db
    restart: unless-stopped
    volumes:
      - postgres_data:/var/lib/postgresql/data/
      - ./scripts/init-db.sql:/docker-entrypoint-initdb.d/init-db.sql:ro
    environment:
      - POSTGRES_USER=${DB_USER:-postgres}
      - POSTGRES_PASSWORD=${DB_PASSWORD:-postgres}
      - POSTGRES_DB=${DB_NAME:-trading_db}
      - TIMESCALE_TELEMETRY=off
    ports:
      - "${DB_HOST_PORT:-5434}:5432"  # 5434 to avoid conflict with local PostgreSQL and other stacks
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - octopus-network

  redis:
    image: redis:7-alpine
    container_name: octopus-redis
    restart: unless-stopped
    ports:
      - "${REDIS_HOST_PORT:-6380}:6379"  # 6380 to avoid conflict with local Redis on 6379
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 3
    networks:
      - octopus-network

  # ============================================
  # BACKGROUND PROCESSING
  # ============================================

  celery-worker:
    build:
      context: .
      dockerfile: Dockerfile.celery
    image: octopus-trading-celery:latest
    container_name: octopus-celery-worker
    restart: unless-stopped
    command: celery -A src.core.celery_app worker -l info -Q default,data_processing,market_data,ml_training,prediction,backtesting,portfolio,risk,strategies,analytics,generative,llm
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
      - ./models:/app/models
    environment:
      - DATABASE_URL=postgresql://postgres:${DB_PASSWORD:-postgres}@db:5432/trading_db
      - REDIS_URL=redis://redis:6379/0
      - CELERY_BROKER_URL=redis://redis:6379/0
      - CELERY_RESULT_BACKEND=redis://redis:6379/0
    depends_on:
      - db
      - redis
    networks:
      - octopus-network
    healthcheck:
      test: ["CMD", "celery", "-A", "src.core.celery_app", "inspect", "ping"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 25s

  celery-beat:
    build:
      context: .
      dockerfile: Dockerfile.celery
    image: octopus-trading-celery:latest
    container_name: octopus-celery-beat
    restart: unless-stopped
    command: celery -A src.core.celery_app beat -l info
    environment:
      - DATABASE_URL=postgresql://postgres:${DB_PASSWORD:-postgres}@db:5432/trading_db
      - REDIS_URL=redis://redis:6379/0
      - CELERY_BROKER_URL=redis://redis:6379/0
      - CELERY_RESULT_BACKEND=redis://redis:6379/0
    depends_on:
      redis: { condition: service_healthy }
      celery-worker: { condition: service_healthy }
    networks:
      - octopus-network
    healthcheck:
      test: ["CMD-SHELL", "pgrep -f 'celery.*beat' || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s

  flower:
    image: mher/flower:2.0
    container_name: octopus-flower
    restart: unless-stopped
    ports:
      - "5555:5555"
    environment:
      - CELERY_BROKER_URL=redis://redis:6379/0
      - CELERY_RESULT_BACKEND=redis://redis:6379/0
      - FLOWER_PORT=5555
      - FLOWER_BASIC_AUTH=admin:admin
    depends_on:
      redis:
        condition: service_healthy
      celery-worker:
        condition: service_healthy
    networks:
      - octopus-network

  # ============================================
  # BASIC MONITORING
  # ============================================

  prometheus:
    image: prom/prometheus:latest
    container_name: octopus-prometheus
    restart: unless-stopped
    ports:
      - "9090:9090"
    volumes:
      - ./monitoring/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus_data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--storage.tsdb.retention.time=15d'
      - '--web.enable-lifecycle'
    networks:
      - octopus-network

  grafana:
    image: grafana/grafana:latest
    container_name: octopus-grafana
    restart: unless-stopped
    ports:
      - "3001:3000"
    volumes:
      - grafana_data:/var/lib/grafana
      - ./monitoring/grafana/datasources:/etc/grafana/provisioning/datasources:ro
      - ./monitoring/grafana/dashboards:/etc/grafana/provisioning/dashboards:ro
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_ADMIN_PASSWORD:-admin}
      - GF_USERS_ALLOW_SIGN_UP=false
      - GF_SERVER_ROOT_URL=http://localhost:3001
    depends_on:
      - prometheus
    networks:
      - octopus-network

  # ============================================
  # LLM REPORT GENERATION (Falcon + FinGPT)
  # Optional: use --profile llm to start (TGI is x86_64 only, not ARM/Mac)
  # ============================================

  tgi-falcon:
    profiles:
      - llm
    image: ghcr.io/huggingface/text-generation-inference:2.0.1
    container_name: octopus-tgi-falcon
    restart: unless-stopped
    ports:
      - "8080:80"
    environment:
      - MODEL_ID=tiiuae/falcon-7b-instruct
      - MAX_BATCH_SIZE=4
      - MAX_TOTAL_TOKENS=2048
    volumes:
      - llm_data:/data
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:80/health || curl -sf http://localhost:80/ || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 180s
    networks:
      - octopus-network

  fingpt-inference:
    profiles:
      - llm
    build:
      context: .
      dockerfile: Dockerfile.fingpt-inference
    image: findash-fingpt-inference:latest
    container_name: octopus-fingpt
    restart: unless-stopped
    ports:
      - "8081:8000"
    environment:
      - HF_TOKEN=${HF_TOKEN:-}
      - HF_FINGPT_MODEL=${HF_FINGPT_MODEL:-distilgpt2}
    volumes:
      - llm_data:/app/cache
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 180s
    networks:
      - octopus-network

volumes:
  postgres_data:
  redis_data:
  prometheus_data:
  grafana_data:
  llm_data:

networks:
  octopus-network:
    driver: bridge 