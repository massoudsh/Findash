# Log

> تاریخچه append-only — هر ورود با `## [YYYY-MM-DD] <type> | <خلاصه>` شروع می‌شود.

## [2026-07-22] feature | صفحه `/data` و `/reports` یکپارچه شدند (additive، هیچ صفحه‌ای حذف نشد): تب سوم «AI Report» به `data/page.tsx` اضافه شد که کامپوننت موجود `ReportsContent` را reuse می‌کند (بدون duplicate logic)، به‌همراه دکمه «Turn this data into an AI-written report» در تب Charts؛ در `reports/page.tsx` هم لینک برگشتی به `/data?tab=charts` اضافه شد. سندباکس Python این نشست کاملاً reset شده بود (هیچ dependency ای نصب نبود، حتی pytest) — نصب کامل `requirements.txt` (شامل torch/transformers/cvxpy/...) به‌عمد انجام نشد چون heavy-build داخل کانتینر ممنوع است؛ چون این تغییر صرفاً frontend (TSX) است و هیچ فایل backend/Python لمس نشد، تست pytest مرتبطی برای رگرسیون وجود نداشت — صحت با بازخوانی دقیق کد و اجزای reuse‌شده (`ReportsContent`, `LlmStatusBadge`) تأیید شد. جزئیات در [[concepts/data-reports-integration]]
## [2026-07-22] feature | AI Asset Allocation Copilot اضافه شد: endpoint جدید `POST /api/copilot/allocation-analysis` (`allocation_copilot.py`) تحلیل rule-based (بدون ML/سیگنال) روی ترکیب دارایی موجود کاربر — HHI concentration index، دیورسیفیکیشن اسکور، insights فارسی + disclaimer صریح «توصیه خرید/فروش نیست» (تمایز محصول از کانال‌های سیگنال‌ده). کامپوننت `AllocationCopilot` داخل `IranPortfolioSection` نمایش داده می‌شود. جزئیات در [[entities/assets-feature]]. تست: `tests/test_allocation_copilot.py` (۳/۳ pass)؛ کل suite بدون رگرسیون (163 passed، فقط ۳ فیل قبلی و نامرتبط در `test_phase4_integration.py`/`enhanced_backtester` که روی `main` هم از قبل fail بودند)
## [2026-07-22] feature | auth واقعاً به جدول `users` در PostgreSQL وصل شد (به‌درخواست کاربر): `professional_auth.py` دیگر از in-memory dict استفاده نمی‌کند؛ همه endpointها (`login`/`register`/`profile`/`list_users`/`refresh`/`change_password`) با `Depends(get_db)` روی مدل `User` واقعی کوئری می‌زنند؛ ۳ اکانت دمو با `_ensure_demo_users()` idempotent seed می‌شوند. مدل `User` گسترش یافت (`role`, `permissions`, `last_login`, `first_name`, `last_name`)؛ `crud.py`'s `get_user_by_email` اضافه شد. باگ جانبی رفع‌شده: دو `declarative_base()` جدا (`models.py` vs `postgres_connection.py`) باعث می‌شد `create_tables()` جدول `users` را نسازد — اکنون هر دو Base ساخته می‌شوند و `initialization.py` این را در startup صدا می‌زند. `tests/test_auth.py` به fixture `client` (SQLite in-memory از `conftest.py`) منتقل شد. نتیجه: 27/27 + کل suite بدون رگرسیون (132 passed در فایل‌های سبک؛ فایل‌های سنگین ML/`test_ingestion_pipeline.py` طبق قبل خارج از scope). مسائل معماری کشف‌شده و مستند (خارج از scope): اسکیمای SQL قدیمیِ orphan (`01_initial_schema.sql`) و migration خالی آلمبیک (`c5dc4ecea576_.py`) — جزئیات در [[concepts/auth-flow]]

## [2026-07-21] fix | `tests/test_main_endpoints.py` (سومین فایل stale مستندشده قبلی) بازنویسی شد: به `/auth/token` (OAuth2 روی `User` DB model) و `/strategies/backtest`+`/strategies/results/{id}` (Celery `AsyncResult` روی ماژول فرضی `api.endpoints.strategies`) اشاره می‌کرد که هیچ‌کدام در اپ وجود ندارند. تصمیم: به‌جای بازسازی router های legacy (که یک feature تکراری/موازی می‌ساخت)، تست‌ها به معادل واقعی موجود منتقل شدند: `/api/auth/login` (professional_auth) و `/api/backtesting/run`+`/results/{id}` (سینک، auth-gated). نتیجه: 163 passed/1 error (فقط `test_ingestion_pipeline.py` نیازمند Postgres واقعی، مستند/خارج از scope)

## [2026-07-21] fix | ناسازگاری bcrypt/passlib در auth رفع شد: `bcrypt>=5.0.0` صفت `__about__` را که `passlib==1.7.4` نیاز دارد حذف کرده بود، پس `hash_password()`/`verify_password()` استثنا می‌داد و در `except Exception` عمومی `professional_auth.py` بلعیده می‌شد → login/register/refresh/profile/logout/api-keys به‌جای 401/400 واقعی، `200` با `success=False` برمی‌گرداندند. رفع: پین `bcrypt==4.1.2` در `requirements/requirements.txt` و `requirements-basic.txt` (هم‌راستا با `requirements-quickstart.txt`) + نصب واقعی. نتیجه: `tests/test_auth.py` کامل ۲۷/۲۷ pass؛ کل suite 154 passed/5 failed/7 error (باقی‌مانده: `test_main_endpoints.py` stale + `test_ingestion_pipeline.py` نیازمند Postgres واقعی، هر دو مستند/خارج از scope)

## [2026-07-21] docs | نقطه ورود واقعی backend شفاف شد: `start.py` (ریشه ریپو، `docker/Dockerfile.fastapi`'s `CMD`) — نه `src/main_refactored.py` مستقیم — سرور production را با validation محیط/dependency بالا می‌آورد و روی `uvicorn.Config(app="src.main_refactored:app", ...)` سوار است؛ `entities/backend.md` به‌روزرسانی شد. همچنین TASK-025 (`b3f05f2`): rate limiting واقعاً enforce می‌شود، schema ساخت API key رفع شد، `IntelligenceOrchestrator` اکنون agentهای واقعی/stub می‌سازد — 154 passed/5 failed/7 error (باقی‌مانده فقط `test_main_endpoints.py` stale و `test_ingestion_pipeline.py` نیازمند Postgres، هر دو مستند و خارج از scope)

## [2026-07-20] fix | TASK-025 ادامه — ۲۰ باگ واقعی auth/cache رفع شد (`17094cf`): `verify_token()` پارامتر `token_type` گم‌کرده بود (رفرش توکن در production همیشه fail می‌شد)؛ auth endpoints همیشه HTTP 200 برمی‌گرداندند به‌جای 401/400؛ `SecurityHeadersMiddleware` هرگز register نشده بود؛ چند endpoint با dict-access روی `TokenData` (pydantic object) کرش می‌کردند (500)؛ `cache.py`'s `cached()` decorator برای توابع sync هم `await` می‌کرد و `sync_wrapper` از `asyncio.get_event_loop()` حذف‌شده استفاده می‌کرد. + رفع تست‌های conftest.py (`full_name` نامعتبر)، pandas `freq='H'`→`'h'`، نصب `statsmodels`. نتیجه: 121 passed/34 failed/11 error → 139 passed/20 failed/1 error. باقی‌مانده مستند در backlog.md: `test_main_endpoints.py` (سومین فایل stale)، `test_ingestion_pipeline.py` (نیاز Postgres)، و gap معماری `IntelligenceOrchestrator` (نیاز `torch`/`prophet`، مستند در [[entities/orchestrator]])
## [2026-07-20] fix | TASK-025 ادامه — exit code 4 رفع شد (`e4a15ca`): بدون export دستی `ENVIRONMENT=development`، `conftest.py` هنگام import کردن `src.main_refactored` باعث `pydantic.ValidationError` روی SECRET_KEY/JWT_SECRET_KEY می‌شد و pytest با exit 4 (usage error) خارج می‌شد نه اجرای واقعی تست‌ها. رفع: `os.environ.setdefault("ENVIRONMENT", "development")` در ابتدای `tests/conftest.py` قبل از import ماژول‌های src؛ حالا suite کاملاً self-contained است. تأیید نهایی: دستور لخت `pytest --tb=short -q` با exit 1 (نتیجه طبیعی تست‌های fail‌شده) تمام می‌شود: 121 passed/34 failed/11 error. `make test` همچنان exit 127 می‌دهد چون باینری `make` بدون root روی این کانتینر نصب‌نشدنی است (محدودیت sandbox، نه باگ کد)
## [2026-07-20] fix | TASK-025 ادامه — `pytest.ini` رفع شد (`bd3edfc`): حتی بعد از رفع باگ `bots_persistence`, دستور لفظی `pytest --tb=short -q` (بدون فلگ) هنوز با collection-abort متوقف می‌شد چون ۲ فایل stale (test_options_trading.py, test_websocket.py) با ImportError کل run را می‌شکستند. راه‌حل: افزودن `--ignore` این ۲ فایل به `addopts` در `pytest.ini` موجود؛ اکنون دستور لخت به نتیجه نهایی (121 passed/34 failed/11 error) می‌رسد بدون abort
## [2026-07-20] fix | TASK-025 — pytest/make exit 127 رفع شد (`7094617`): علت اصلی نصب‌نبودن deps + باگ واقعی `src/api/bots_persistence.py` مفقود (کل app import را می‌شکست). بعد از رفع: 121 passed/34 failed/11 error از 165 تست (بدون 2 فایل stale: test_options_trading.py، test_websocket.py که به ماژول‌های حذف‌شده `src.main`/`src.core.websocket` اشاره می‌کنند). `make` بدون root قابل نصب نیست؛ جایگزین: `ENVIRONMENT=development python3 -m pytest tests/`
## [2026-07-20] fix | TASK-008 رفع شد (`335019e`) — font-family اشتباه `IRANYekanX` (که فایل واقعی‌اش هرگز وجود نداشت) به `Dana` تغییر نام یافت در globals.css/tailwind.config.ts/risk-gauge.tsx؛ فایل‌های src از قبل به‌درستی Dana بودند، فقط نام گمراه‌کننده بود. راستی‌آزمایی: TASK-009/014 placeholder strings واقعاً حذف شده‌اند (dashboard از IranMarketOverview/AnalyticsOverview واقعی استفاده می‌کند)
## [2026-07-19] feature | تب «تحلیل بازار» به `/trading` (مرکز فرماندهی) اضافه شد — ۶ زیرتب (تکنیکال/کلان/بنیادی/آن‌چین/اجتماعی/مدل‌های AI) که صفحات مستقل موجود را با `lazy()`+`Suspense` بارگذاری می‌کنند؛ صفحات مستقل حذف نشدند (additive). ترجمه فارسی متن داخل آن صفحات هنوز انجام نشده
## [2026-07-19] feature | ماژول داخلی «استارتاپ‌تراکر» ساخته شد — backend `src/api/endpoints/startup_tracker.py` (CRUD فرضیه GTM/مکالمه مشتری/traction + summary، in-memory) + tab جدید در `/admin` با `startup-tracker-panel.tsx` (فرم‌ها، لیست، حذف، لینک مکالمه به فرضیه)؛ API smoke-test شده با TestClient
## [2026-07-08] sync | وضعیت backlog sync شد — TASK-007,009-024 ✅ Done (commits 7641e3e & 8e6bcc3) | فقط TASK-008 (فونت IRANYekanX) باقی است
## [2026-07-07] feature | مانوال فارسی کامل (help page 4 tab) + tab integration trading page + social/alerts headers
## [2026-07-07] localization | فارسی‌سازی کامل پلتفرم — نام/شخصیت ۱۱ عامل (M1-M11) به فارسی، tabs و panel‌های trading، source labels، mock data، timestamp‌ها و aria-labels همه ترجمه شدند
## [2026-07-06] feature | قابلیت افزودن دارایی: AddAssetModal (15 نماد ایرانی، auto-calc، localStorage) + IranPortfolioSection (donut chart SVG، holdings list، تاریخچه) — وصل به portfolio tab
## [2026-07-04] health-check | بررسی تناقضات ویکی — رفع 4 مورد: agents count (11→M1-M5 documented+M6-M11 planned)، Redis TTL (clarify 300s vs 60s)، TASK-001b/f (open→done)، font (overview اصلاح شد)
## [2026-07-01] redesign | داشبورد `/dashboard` با سبک modern mill-flat و رنگ `#3B82F6` بازطراحی شد — grid KPI cards، SVG performance chart، donut allocation، positions table، widgets و activity timeline
## [2026-07-01] audit | آدیت کامل Hawk View: 18 تسک جدید در backlog — Auth broken (demo hardcoded)، font missing، market/analytics tab placeholder، Iranian assets absent، CI/CD placeholder، no Jalali calendar
## [2026-06-27] bootstrap | ویکی پروژه اختاپوس از README bootstrap شد — overview، index، 4 entity، 2 concept ساخته شد
## [2026-06-27] backlog | بک‌لاگ پروژه ساخته شد — TASK-001 (دارایی‌های ایرانی) با اولویت بالا + 5 تسک دیگر
## [2026-06-27] feature | TASK-001 پیاده‌سازی شد — 11 فایل جدید: schemas، models، service، API router، migration SQL، 6 فایل frontend
## [2026-06-27] feature | TASK-001 کامل شد — 001e widget داشبورد، 001f portfolio section، 001g main_refactored.py ساخته شد
## [2026-06-28] feature | TASK-002~006 کامل شد — IranMacro، CurrencyToggle، Jalali، backtesting، 2 test file، README update
## [2026-06-29] feature | 6 قابلیت جدید — WebSocket hook، TradeTracker، PriceAlerts، News page، light-mode CSS، 31 unit test، build ✓ 57 pages
## [2026-06-29] update | تغییر پورت frontend از 3000 به 3003 (Dockerfile, docker-compose-core, docker-compose-complete, package.json, Makefile)
## [2026-06-30] update | فارسی‌سازی کل اپ: lang=fa dir=rtl، فونت Vazirmatn، default locale=fa، fa-utils.ts، ترجمه صفحات auth/dashboard/error و ۲۰+ کامپوننت
## [2026-06-30] feature | یکپارچه‌سازی زرین‌پال: backend create/callback/verify، payment_orders SQL، صفحات checkout/success/failed/callback و env sandbox
## [2026-06-30] redesign | بازطراحی کامل UI/UX فارسی: IRANYekanX، سبز #22C55E، persian-card/border/pattern، داشبورد 5 تب یکپارچه، RiskGauge SVG، CreditScore، homepage phone mockup، README فارسی کامل
## [2026-07-22] docs | trace کامل login flow (signin → NextAuth → /api/auth/login → in-memory demo users) + صفحه جدید concepts/auth-flow.md؛ کشف: لاگین به PostgreSQL واقعی وصل نیست
## [2026-07-22] fix | باقی‌مانده TASK-025 کامل شد: ۲ فایل stale بازنویسی شدند و از `--ignore` در `pytest.ini` حذف شدند — `test_options_trading.py` (فقط منطق تجاری واقعی `options_risk_integration.py`؛ لایه HTTP فرضی قدیمی حذف شد چون هیچ‌وقت mount نشده) و `test_websocket.py` (مطابق API واقعی `WebSocketManager`/`unified_websocket.py`؛ کشف رفتار واقعی endpoint: دو پیام `connection_established` پشت‌سرهم می‌فرستد). هر ۴۴ تست جدید pass. + کشف و رفع باگ جدی جداگانه: کرش `RuntimeError: no running event loop` در import اپ وقتی torch/transformers واقعاً نصب باشند (جزئیات [[entities/orchestrator]]). یافته مستندشده-نه-رفع‌شده: `fastapi` در `requirements.txt`/`requirements-basic.txt` بدون پین نسخه است؛ نصب تازه، جدیدترین fastapi (۰.۱۳۹.۲) + starlette (۰.۵۲.۱) را می‌گیرد که با `prometheus-fastapi-instrumentator==6.1.0` ناسازگار است (`AttributeError: '_IncludedRouter' object has no attribute 'path'` روی هر درخواست HTTP واقعی) — باعث fail شدن test_auth.py/test_main_endpoints.py/test_phase2_api.py/test_allocation_copilot.py در این session شد؛ رفع نیازمند پین‌کردن نسخه سازگار fastapi و تأیید روی سرور SSH است (خارج از scope این session). test_phase4_integration.py هم به دلیل ناسازگاری نسخه `statsmodels`/`pandas` (`deprecate_kwarg` TypeError) fail می‌شود — مستقل و جدا از تغییرات این session.
